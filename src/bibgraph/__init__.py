"""bibgraph: a local, reproducible bibliography-graph pipeline.

Standard library only. The optional `pdftotext` system command is the single
external adapter, and its absence is reported rather than worked around.
"""

__version__ = "0.1.0"
